//TODO hard drive failure deleted this solution
pub fn p1(s: &str) -> String {
    0.to_string()
}

pub fn p2(s: &str) -> String {
    0.to_string()
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_p1() {
        let input = "";
        assert_eq!(p1(input), 0.to_string());
    }

    #[test]
    fn test_p2() {
        let input = "";
        assert_eq!(p2(input), 0.to_string());
    }
}
